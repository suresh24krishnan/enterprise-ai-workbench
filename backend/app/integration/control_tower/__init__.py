"""
Control Tower — observability layer for supervised tool executions.

Captures supervisor run metadata for operational visibility and demo.
In-memory only (LAB). No secrets, no document/email bodies, metadata only.
"""
